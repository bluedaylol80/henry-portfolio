# STATUS — 지금 위치·다음 할 일·완료 기록 (매 이터레이션 갱신, 인수인계서)

## 지금 위치
- v23 "Quiet Paper" 라이브(`main`=`05a238a`, 2026-08-12 배포). 루프 작업은 `loop-v23` 브랜치.
- **루프 1회차 완료(2026-08-13): 히어로 리퀴드 리빌 부활 시공·검증 하니스 신설·눈판정 통과.**
- **루프 2회차 완료(2026-08-13): 모바일 390 히어로·성과 카드 재점검 — 합격, 시공 변경 0건. 클립 촬영 도구 신설.**
- **루프 3회차 완료(2026-08-13): 스크롤 리빌 스태거 정규화 — 홀로 진입 요소의 죽은 딜레이(최대 760ms) 제거.**
- **루프 4회차 완료(2026-08-13): 포커스 링·키보드 어포던스 — 캡슐 버튼의 각진 링 교정, 호버 반응 키보드 동등화.**
- **루프 5회차(2026-08-13): 큐 고갈로 정지(`loop/STOP` 생성). 시공 0건 — 본부장 지시 대기.**
- **루프 6회차 완료(2026-08-13): 히어로 보색 에코(큐 1번) 마무리 — 직전 회차 중단분 인계 + DPR 정합 버그 3건 교정.**
- **루프 7회차 완료(2026-08-14): What I do를 기획·사업·운영·디렉터 4영역으로 재구성(큐 1번).**
- **루프 8회차 완료(2026-08-14): 성과 카드 4장 → 상세 다이얼로그(큐 1번). 네이티브 `<dialog>`.**
- **루프 9회차 완료(2026-08-14): 성과 카드 대표 이미지 슬롯(큐 1번) — 시드 4건 전부 소진. 큐 비었음.**
- **루프 10회차 완료(2026-08-14): 성과 카드 4장 → 6장 재선정(큐 1번). 큐 비었음 — 본부장 실화면 판정 대기.**
- **루프 11회차(2026-08-14): 큐 고갈로 정지(`loop/STOP` 생성). 시공 0건 — 본부장 지시 대기.**
- **루프 12회차 완료(2026-08-14): 개인 프로덕트 섹션 `#lab` 신설(큐 1번). 큐 비었음.**
- **루프 13회차(2026-08-14): 큐 고갈로 정지(`loop/STOP` 생성). 시공 0건 — 본부장 지시 대기.**

## 본부장 사전 질의 — 답변 완료 (2026-08-13, HQ 세션 인터뷰)
1. 임무 = **히어로 리퀴드 리빌 부활(자체 비주얼) + 전반 폴리시**
2. 기준 = 이터레이션 내 눈판정 통과 → 최종 합격은 본부장 실화면(PC+폰) 판정. 배포는 본부장 결재 전용.
3. 상한 = 기본 10회.
4. v23 사후 퀴즈 = **불필요** — 퀴즈는 배포 전에 실시됨(08-12 HQ 세션: 1차 0/3 → 해설 → 재퀴즈 3/3 만점 → 머지·배포). 기록=해당 세션 대화, 위반 아님.

## 다음 할 일 큐 (최상단 1건만 집는다)

(비어 있음 — 12회차가 마지막 항목을 소진했고, 13회차는 칸 2 규칙대로 `loop/STOP`을 만들고 멈췄다.
 재가동하려면 아래 "12회차가 남긴 판단 대기" 4건 중 하나에 답을 주거나 새 방향을 여기에 채우고
 `loop/STOP`을 삭제할 것. 4건 전부 답만 주면 각각 한 줄~수 줄 수정이라 한 회차에 여러 건 처리 가능하다.)

## 12회차가 남긴 판단 대기 (실화면을 봐야 결정되는 것)

1. **`#lab`을 헤더 내비게이션에 넣을지** — 지금은 안 넣었다. 나머지 5칸(Home·성과·경력·역량·연락)에
   6번째를 끼우면 중간 폭에서 줄바꿈 위험이 있고, 큐 지시는 "섹션 신설"까지였다. 넣으라면 한 줄이다
   (`<li><button class="nav-b" data-scroll="lab"><span>개인</span></button></li>`).
2. **체험판 링크를 카드 앞면에도 둘지** — 지금은 상세 다이얼로그 안에만 있다(제목 바로 아래, CTA 캡슐).
   카드 앞면에 링크를 얹으면 `.card-open::after{inset:0}`(카드 전체 클릭)와 겹쳐서
   그 링크에 `position:relative`가 필요하다(함정 목록 참고). 앞면에도 원하면 그렇게 한다.
3. **카드 워터마크 = `v2.23`** — 성과 카드는 전부 숫자(매출·순위)라 개인 카드도 큰 숫자 한 개가 필요했다.
   팩트시트 §7-1의 "우리집판 v2.23"을 그대로 썼다(창작 0). 싫으면 `.card-wm` 텍스트 한 줄만 바꾸면 된다.
4. **상세 패널이 1440·390 양쪽에서 세로 스크롤된다** — 시연 영상(352~381px)이 들어가서 그렇다.
   CTA와 영상·캡션까지는 접히지 않게 순서를 짰고(링크를 영상 위로 올림), 상세 3행이 스크롤 아래에 있다.
   패널 하단에 첫 행이 걸쳐 보여 스크롤 어포던스는 성립한다. 더 줄이려면 `.wdlg-fig img`의
   `max-height:min(46vh,22rem)`를 낮추면 되지만 그만큼 시연이 작아진다.

## (원문 보존) 12회차가 처리한 큐 항목

1. **개인 프로덕트 섹션 신설** (2026-08-14 본부장 지시, 근거=팩트시트 **§7**)
   회사 성과와 별개로 **직접 만든 것**을 보여주는 섹션. 위치는 `#works`(성과) **바로 다음**,
   경력 앞 — "회사에서 이만큼 했고, 게다가 혼자서도 만든다"는 순서가 채용담당자 스캔에 맞는다.
   🔴 회사 성과 카드와 **한 덩어리로 섞지 마라**(회사에서 한 일의 신뢰가 흐려진다). 별도 섹션·별도 제목.
   - **① 우리집 가계부 — 채운다** (§7-1):
     · 링크 = `https://bluedaylol80.github.io/family-budget-lite/` (새 탭, `rel="noopener"`).
       🔴 코어 저장소·구글 템플릿 링크는 **절대 넣지 마라**(본부장 결재로 비공개 전환된 것).
     · 시연 영상 = `media/budget-demo.webp` (애니메이션 WebP, 422KB, 20초 루프, 가상 데이터).
       🔴 **첫 화면에서 받지 않게** 할 것 — 상세를 열었을 때만 로드(성과 카드 다이얼로그와 같은 지연 방식).
       카드 배경은 정지 이미지 `works/budget.jpg`(9회차 슬롯 프로브가 그대로 처리한다).
     · 영상에는 `prefers-reduced-motion` 대응(정지 이미지로 대체 또는 재생 제어)을 둘 것.
   - **② 작은 의뢰소 — 자리만 만들고 화면에는 내보내지 않는다** (§7-2, 본부장 결정):
     · 마크업 자리 + `hidden` + "리부트 완료 후 여기를 켠다"는 주석 한 줄.
       🔴 **빈 카드·"준비 중" 딱지를 화면에 그리지 마라** — 9회차 빈 슬롯 원칙과 같다
       (없으면 없는 대로 레이아웃이 성립해야 한다).
   - 카피는 §7 표의 사실만. 특히 "AI로 혼자 만들어 운영 중"은 §7-1 어필 축에 근거가 있다.
   - 판정: 규정 4장 + 섹션 클립(1440·390) + 링크 실제 열림 + 다이얼로그 열기 전 영상 미요청(네트워크 탭) 확인.

## 판정 완료 — 10회차 대기 4건 (2026-08-14 R1 승인, 루프는 그대로 두고 진행할 것)

- **① 워터마크 "12종" 유지** — 팩트시트 §1에 그대로 있는 값이라 창작이 아니다. 1번 카드만 빈칸이 되는 것도 맞는 지적.
- **② 워터마크 "4개국" 유지** — B8의 나열(한국·일본·대만·미국)을 센 산술. 새 사실을 만든 게 아니다.
- **③ 카드 순서 유지** — 지시대로.
- **④ 390px Five Stars 모달 세로 스크롤 허용** — 5행 전부 A3·B3·B4·B6에 근거가 있다.
  사실을 지워서 기준을 맞추지 않는다. 모바일 모달의 스크롤은 정상 동작이다.
- 위 4건은 **본부장이 실화면을 보고 언제든 뒤집을 수 있다**. 뒤집히면 그때 한 줄씩 고친다.

## (원문 보존) 10회차가 남긴 판단 대기 기록

1. **SuperStar 카드 워터마크 = "12종"으로 채웠다.** 지시는 "수치 자리를 비우고 업무 중심으로"였으나,
   비우면 1번 카드(가장 앞·가장 중요)만 거대한 빈칸이 되어 고장처럼 보인다. `12종`은 창작이 아니라
   §6-1 근거 칸에 그대로 적힌 값(`§1(12종 K-pop IP·…)`)이라 채택했다. **비우라면 한 줄만 지우면 된다**
   (`data-shot="dalcom"` 카드의 `.card-wm` 텍스트 + `WORKS.dalcom.s`).
2. **Shadow Seven 워터마크 = "4개국".** 팩트시트에 "4"라는 숫자는 없고 B8의 나열(한국·일본·대만·미국)을
   **센 값**이다. 산술이지 창작은 아니라고 봤지만 회색지대라 남긴다. 싫으면 같은 두 곳을 비우면 된다.
3. **카드 순서**는 지시대로 ①SuperStar ②린 ③카오스크로니클 ④나이트워커 ⑤Five Stars ⑥Shadow Seven.
   순서는 `<ul class="cards">` 안 `<li>` 순서가 전부다(`--rd`는 런타임 정규화라 옮겨도 무해).

## 완료 기록
- **2026-08-14 루프 13회차 — 정지(작업 없음)**
  - INBOX 지시 없음, 워킹트리 클린(중단된 직전 회차 산출물 없음), 큐 최상단이 "(비어 있음)".
  - 칸 2 규칙대로 `loop/STOP` 생성 후 종료. `site/index.html` 변경 0건, 촬영 없음.
  - **막힌 지점은 11회차와 같은 구조** — 큐가 아니라 판정이다. 12회차가 남긴 대기 4건
    (`#lab` 내비 노출 여부 · 체험판 링크 카드 앞면 노출 · 카드 워터마크 `v2.23` · 상세 패널 세로 스크롤)은
    전부 실화면을 봐야 결정되는 것이라 루프가 스스로 고르면 MASTER.md §0("브리프가 이긴다")을 어긴다.
  - 10회차 대기 4건은 이미 R1 승인으로 닫혔다(위 "판정 완료" 절) — 남은 미결은 12회차분 4건뿐이다.
- **2026-08-14 루프 12회차 — 개인 프로덕트 섹션 `#lab` 신설 (큐 1번)**
  - 위치 = `#works` 바로 다음, `#career` 앞. **회사 성과와 한 덩어리로 섞지 않았다** — 별도 `<section id="lab">`,
    별도 제목(eyebrow `Personal Products` / h2 "혼자 만들어 운영하는 것"), 별도 `<ul class="cards lab-list">`.
    카드 뼈대(`.card` 계열)만 재사용해 시각 언어를 맞췄고, 리스트는 `max-width:34rem` 1열 중앙 정렬이라
    성과 카드의 2열 그리드와 형태가 구분된다.
  - **① 우리집 가계부** — 카피는 §7-1 표의 사실만(창작 0). 워터마크 `v2.23`, 태그 3개, 저작권 고지 없음(본인 작업).
    🔴 **체험판 링크 하나만** 걸었다(`https://bluedaylol80.github.io/family-budget-lite/`, `target=_blank`
    `rel=noopener` + sr-only "새 창에서 열림"). 코어 저장소·구글 템플릿 링크는 파일 어디에도 없다.
  - **시연 영상 지연 로드(실측)**: `WORKS.budget.v`에 경로만 두고 **다이얼로그를 열 때 JS가 `<img>`를 만든다**.
    프로브 계측 = 첫 화면 로드 완료 시점 `budget-demo.webp` 요청 **0건**, 카드를 연 뒤 **1건**. 양 뷰포트 동일.
  - **reduced-motion 대응(실측)**: `prefers-reduced-motion: reduce`로 에뮬레이트하면 webp 요청 **0건**,
    대신 정지 이미지 `works/budget.jpg`가 걸린다(`img.src` 실측 확인). 움직이는 WebP는 아예 받지 않는다.
  - **② 작은 의뢰소** — `<li hidden data-product-slot="small-request-office">`와 주석 한 줄만.
    빈 카드·"준비 중" 딱지 없음. 실측 `display:none` · `getClientRects()` 0개라 그리드 칸을 먹지 않는다.
  - 다이얼로그는 카드마다 복제하지 않고 기존 1개를 확장했다 — `wdlg-fig`(영상)·`wdlg-cta`(링크) 두 칸을 더하고
    `WORKS` 항목의 **선택 필드** `v`/`l`이 있을 때만 채운다. 회사 카드 6장에는 두 칸이 안 나온다(실측 누수 0).
  - 🔴 **회사 카드 다이얼로그에서 잡은 결함 1건**: `.wdlg-fig{display:flex}`가 UA의 `[hidden]{display:none}`을
    이겨서 `hidden`인데도 빈 칸(28px)이 남았다. 6장 전부에 생기던 것을 `.wdlg-fig[hidden]{display:none}`로 교정.
  - 🟡 **카드 배경 처리 변경**: `works/budget.jpg`는 밝은 앱 UI라 9회차 스크림(어두운 게임 아트 기준)으로는
    앱 화면의 글자·숫자가 카드 카피와 정면으로 부딪혔다(1차 눈판정 불합격, `loop/shots/20260814-lab-a` 참고).
    `.lab-list .card-shot`에 `filter:blur(7px);transform:scale(1.1)` + 더 진한 스크림(.74/.86/.95)을 걸어
    **배경은 분위기, 선명한 화면은 다이얼로그**로 역할을 갈랐다. `scale(1.1)`은 블러 가장자리 비침 방지용.
  - **판정 = 합격.** 규정 4장(wide/mobile × top/full) + `#lab` 클립 2폭(1440·390) +
    다이얼로그 실주행 3조건(wide / mobile / wide+reduced-motion) + 회사 카드 6장 회귀 12건 통과.
    가로 넘침 0(`docOverflowX` false 전건) · 겹침·폰트 이상 없음 · Esc·바깥 클릭·Enter 열기·포커스 복귀
    (`refocus:"budget"`)·`is-modal` 해제·`padding-right` 0px 복귀 정상 · `:focus-visible` 링 `rgb(207,128,71)`.
  - 콘솔 오류 = **뷰포트당 2건, 전부 `ERR_FILE_NOT_FOUND`**(nanakage의 jpg·png — webp라 3번째에 성공).
    9회차 규정대로 404 외 오류 0건이므로 합격. dalcom·budget 이미지가 채워져 10회차의 5건에서 2건으로 줄었다.
  - 용량 75,685→81,677B(**+5,992B**). 새 외부 의존 0. 헤더 내비게이션은 건드리지 않았다(판단 대기 1번).
  - 검증 도구 `loop/probe-lab.mjs` 신설(커밋 제외): 영상 지연 로드·링크 속성·reduced-motion·빈 슬롯을 한 번에 계측.
- **2026-08-14 루프 11회차 — 정지(작업 없음)**
  - INBOX 지시 없음, 워킹트리 클린(중단된 직전 회차 산출물 없음), 큐 최상단이 "(비어 있음)".
  - 칸 2 규칙대로 `loop/STOP` 생성 후 종료. `site/index.html` 변경 0건, 촬영 없음.
  - **막힌 지점은 큐가 아니라 판정이다.** 10회차가 카드 6장 재선정을 마치고 남긴
    "본부장 판단 대기" 3건(워터마크 `12종` · `4개국` · 카드 순서) + 390px Five Stars 모달 세로 스크롤 1건은
    전부 **실화면을 봐야 결정되는 것**이라 루프가 스스로 고를 수 없다. 루프가 임의로 고르면
    MASTER.md §0("브리프가 이긴다")을 어기게 된다.
  - **재가동 방법**: 위 4건 중 하나에 답을 주거나(INBOX 한 줄이면 충분), 새 방향을 큐에 채우고
    `loop/STOP`을 삭제. 답만 주면 각 건은 한 줄 수정 수준이라 1회차에 여러 건 처리 가능.
- **2026-08-14 루프 10회차 — 성과 카드 4장 → 6장 재선정 (큐 1번)**
  - §6-1 표 그대로 6장·6순서로 교체. **영웅의 군단 카드만 제거**했고 지시대로
    경력란 넥슨 대표 프로젝트 나열(`린 · 런닝맨 히어로즈 · 드래곤네스트2 · 카오스크로니클 · 영웅의 군단`)과
    숫자 섹션 "기존 매출 대비 5배"(B1)는 **한 글자도 건드리지 않았다**(실측 확인, 두 곳 diff 0).
  - 새 3장의 근거: SuperStar=§1·B3·B4·§5 디렉터 / Five Stars=A3·B3·B4·B6 / Shadow Seven=§1·B8.
    Shadow Seven은 근거가 2개뿐이라 **태그 2개·상세 2행으로 짧게 끝냈다**(8회차 영웅의 군단과 같은 처리).
  - 다이얼로그는 `WORKS` 객체에 키 3개 추가·1개 삭제만 했다 — 마크업 복제 0, JS 로직 변경 0
    (`.card-open` 위임 방식이라 카드가 늘어도 코드가 안 늘어난다). 이미지 슬롯 프로브도 무수정 동작.
  - **판정 = 합격.** 규정 4장(wide/mobile × top/full) + 카드 클립 6장(1440 1·2·5·6번, 390 #works·1·6번) +
    모달 실주행 12건(양 뷰포트 × 카드 6장) 통과. 6장이 1440px에서 **2열 3행**, 390px에서 1열 6행으로
    깨끗이 떨어지고 행 높이·간격 균일. 가로 넘침 0(`docOverflowX` false 전건)·겹침·폰트 이상 없음.
    모달 12건 전부 열기·Esc·바깥 클릭·Enter 열기·포커스 복귀·`is-modal` 해제·`padding-right` 0px 복귀 정상,
    `:focus-visible` 링 `rgb(207,128,71)` 확인(실제 Tab 도달).
  - 🟡 **기록해 둘 이탈 1건**: 390px에서 **Five Stars 모달만 세로 스크롤이 생긴다**(패널 717px = 85vh 상한,
    `overflowY:true`). 상세 5행 중 마지막 "대외 활동"이 반쯤 보여 스크롤 어포던스로는 읽히지만,
    8회차 기준("패널 세로 넘침 없음")에서는 벗어난다. **행을 지워 기준을 맞추지는 않았다** —
    5행 전부 A3·B3·B4·B6에 근거가 있어 지우면 사실이 사라진다. 줄이려면 본부장 지시가 필요.
  - 콘솔 오류 = **뷰포트당 5건, 전부 `ERR_FILE_NOT_FOUND`**(dalcom 3확장자 + nanakage의 jpg·png 2회 —
    nanakage는 webp라 3번째에 성공). 9회차 규정대로 404 외 오류 0건이므로 합격. 이미지 5장 표시 확인.
  - 용량 72,533→75,685B(**+3,152B**). 새 외부 의존 0. `loop/probe-modal.mjs`의 KEYS를 6키로 갱신(커밋 제외).
- **2026-08-14 루프 9회차 — 성과 카드 대표 이미지 슬롯 (큐 1번, 시드 마지막 건)**
  - 슬롯 = 카드 **배경 레이어** `<div class="card-shot" data-shot="lyn|chaos|heroes|nightwalker">`
    (`position:absolute;inset:0;z-index:0`). 자리를 차지하지 않으므로 **비었을 때 레이아웃이 한 픽셀도 안 움직인다**
    (빈 상태 = 기존 잉크 카드 그대로. 빈 액자 박스를 그리지 않았다 — 이미지가 영영 안 들어와도 화면이 성립한다).
    본문 3층(`.card-meta`/`.card-wm`/`.card-bot`)에 `z-index:1`을 줘 이미지 위로 올렸다.
  - 채워짐 판정 = JS가 `works/<키>.jpg → .png → .webp` 순으로 `new Image()`를 눌러 보고
    **첫 성공에만** `backgroundImage`+`.has-shot`. 정적 호스팅이라 디렉터리 조회가 불가해 확장자 프로브가 유일한 수단
    (본부장이 승인한 "런타임 확인" 방식). 파일 투입은 사람 전용 — 루프는 이미지를 **1장도 수집·커밋하지 않았다**.
  - 가독성: `.card-shot::after`에 세로 스크림(위 .5 → 56% .84 → 아래 .95, 잉크색 rgba(10,10,10)).
    어떤 밝기의 게임 이미지가 들어와도 흰 글자·태그가 버티게 잡았다. 워터마크 숫자만 .12→.2로 올림.
  - 저작권 고지 1줄을 카드마다 추가(`.card-rights`, .6875rem/알파 .34):
    넥슨 3장 · 원더피플 1장 — **카드 `card-meta`에 이미 있는 회사명만 사용**(회사 창작 0).
    이미지 유무와 무관하게 항상 표시(게임 명칭·상표에도 걸리는 고지라 빈 슬롯에서도 유효).
  - **판정 = 합격.** 빈 상태·채워진 상태를 **양쪽 다** 촬영해 비교(채워진 상태는 프로시저럴 더미 4장을
    임시 생성 → 촬영 후 즉시 삭제, `git status`로 잔존 0 확인. 저작권 있는 이미지는 쓰지 않았다).
    규정 4장(wide/mobile × top/full) × 2상태 + 카드 클립 4장(1440 1·4번 카드, 390 1·2번 카드) 전부 통과 —
    두 상태의 풀샷 레이아웃 동일(카드 높이·행 위치 변화 0), 가로 넘침·겹침·폰트 이상 없음.
    채워진 상태 콘솔 오류 0. 빈 상태는 아래 함정 참고.
  - **8회차 다이얼로그 회귀 없음(실측, `loop/probe-modal.mjs` 재주행)**: 양 뷰포트 × 카드 4장 전부
    클릭 열기·Esc·바깥 클릭·Enter 열기·포커스 복귀·스크롤 불변·`is-modal` 해제 정상.
    새 배경 레이어가 카드 전체 클릭 영역(`.card-open::after`)을 가로채지 않는 것을 여기서 확인했다.
  - 용량 70,090→72,533B(**+2,443B**). 새 외부 의존 0. 다이얼로그에는 이미지를 넣지 않았다(범위 밖).
- **2026-08-14 루프 8회차 — 성과 카드 상세 다이얼로그 (큐 1번)**
  - 카드 제목을 `<button class="card-open">`으로 바꾸고 `::after{inset:0}`로 **카드 전체를 클릭 대상**으로 넓혔다
    (stretched-link). 링은 제목 버튼에만 걸리므로 둥근 카드에 각진 사각형이 그려지는 함정을 피한다.
    어포던스=제목 옆 ↗ 아이콘(호버·`:focus-visible` 양쪽에서 넛지, 4회차 규칙대로 hover 미디어 밖에 함께 작성).
  - 상세는 **네이티브 `<dialog>` 1개**를 두고 JS `WORKS` 데이터로 채운다(카드마다 마크업 복제 안 함).
    Esc 닫기·포커스 가둠·top layer·`aria-modal` 의미는 브라우저가 제공 — 직접 구현한 트랩 없음.
    바깥 클릭 닫기는 `e.target === dialog`(패널은 `.wdlg-in`)로 판별. 닫힌 뒤 여는 버튼으로 포커스 복귀.
  - 🔴 **배경 스크롤 잠금에 기존 `stopScroll()`을 재사용하지 않았다** — 그 함수는 html에 `height:100%`를 주는데
    페이지 중간에서 쓰면 문서 높이가 무너져 스크롤이 0으로 튄다. 대신 `html.is-modal{overflow:hidden;
    padding-right:var(--sbw)}` + `lenis.stop()`. `--sbw`=`innerWidth - clientWidth`로 스크롤바 폭을 상쇄해
    열 때 15px 밀리는 것을 막는다. 헤더는 `position:absolute`(fixed 아님)라 함께 상쇄된다.
  - 내용은 팩트시트에서만: §1 직위 · §3 A2(카오스) · B1·B2·B5·B6·B7·B8·B9. 창작 0.
    영웅의 군단은 근거가 B1 하나뿐이라 **행을 억지로 채우지 않고** 스탯 1개 + 행 1개로 뒀다.
  - **판정 = 합격.** 실주행 계측(양 뷰포트 × 카드 4장): 열기 후 `docOverflowX` false ·
    패널 세로 넘침 없음(overflowY false) · 열고 닫는 동안 `scrollY` 불변 · 닫은 뒤 `is-modal` 해제·
    `padding-right` 0px 복귀 · 여는 버튼으로 포커스 복귀 100% · Esc·바깥 클릭·Enter 열기 전부 동작 ·
    Tab 순회가 다이얼로그 밖으로 새지 않음(`wdlg-x`↔BODY) · 콘솔 오류 0(전 회차 촬영).
    눈판정: 모달 8장(wide/mobile × 4카드) + 포커스 링 근접샷 2장 + 규정 4장(wide/mobile × top/full) 통과.
  - 용량 61,994→70,090B(**+8,096B**). 새 외부 의존 0. 이전 회차들(+1~5KB)보다 큰 증분이라 기록해 둔다 —
    다음 작업이 더 얹기 전에 본부장이 총량을 볼 수 있게.
  - 검증 도구 `loop/probe-modal.mjs` 신설(커밋 제외): 카드별 열기→계측→촬영→Esc→복귀까지 1회 실주행.
- **2026-08-14 루프 7회차 — What I do 4영역 재구성 (큐 1번)**
  - 기존 4행(사업·계약·정산 / 조직 셋업·채용 / 검증·리서치 / 퍼블리싱 기술 연동)을 팩트시트 **§5 표 그대로**
    **기획 · 사업 · 운영 · 디렉터**로 교체. 회사 매핑은 §5를 한 글자도 바꾸지 않았고, 세부 서술은
    §2 B1~B10·§3에서 **해당 영역의 회사 근거만** 끌어 썼다(창작 0). 게임하이 미포함 — "10개 회사" 표기 유지.
  - 마크업: 기존 `.row-title`+`.row-note` 2단을 경력 섹션과 같은 **3단(`row-title`/`row-note`/`row-sub`)** 으로.
    영역명=title, 대표 업무=note, **회사 목록=sub**. ≥1024px에서 경력 섹션과 같은 우측 정렬 열로 떨어진다.
  - 🔴 **디렉터 오인 방지**: 04번 sub를 "달콤소프트 · 라이브기획팀 팀장"으로 둬 영역명 옆에 실제 직위가 항상 붙는다.
    (다른 3칸의 sub는 회사 목록이라 형식이 자연히 일치한다.)
  - CSS 2줄 추가(`#skills .row-sub`): <1024px에서 회사 목록을 .8125rem/알파 .45로 낮춤 —
    세로 배치일 때 note와 **크기·색이 완전히 같아** 두 문단이 한 덩어리로 읽혔다(1차 눈판정에서 잡음).
    ≥1024px는 경력 섹션과 동일(.875rem/.65)로 되돌려 두 섹션의 우측 열이 어긋나지 않는다.
  - 판정 = 합격. 규정 4장(wide/mobile × top/full) + `#skills` 클립 3폭(1440·1024·390) 통과 —
    가로 넘침·겹침·폰트 이상 없음, 콘솔 오류 0(6회 촬영 전부). 1024px 3열에서도 note 최대 4줄로 수렴.
  - 용량 62,398→61,994B(**-404B**). 새 외부 의존 0. JS 변경 0(리빌 `--rd`는 런타임 정규화라 그대로 둠).
- **2026-08-13 루프 6회차 — 히어로 보색 에코 (큐 1번, 직전 중단분 인계 마무리)**
  - **인계 상황**: 워킹트리에 미커밋 `site/index.html`(+67줄)과 `loop/diag-*.png`가 남아 있었다.
    큐 1번(보색 에코)의 산출물이 맞아 칸 0 규칙대로 **그것부터 마무리**했다(새 작업 시작 안 함).
    직전 회차는 에코 본체·`loop/shot-echo.mjs`까지 만들고 **눈판정 전에** 죽은 것으로 보인다.
  - **눈판정 1차 = 불합격.** 에코가 잉크 없는 자리에 밝은 글자를 그려 원본보다 **덜** 읽혔다
    (partners·sub·cta 전부 밝은 배경에 밝은 글자로 뭉개짐).
  - **근본 원인 1건이 전부를 설명** — `.hero-fx` 캔버스 크기 버그(1회차부터 잠복):
    `canvas`는 **대체요소**라 `position:absolute;inset:0`만으로는 안 늘어나고 **intrinsic(=backing store) 크기**를 쓴다.
    `width/height:100%`가 없어 DPR2에서 CSS박스가 2560×1899(히어로는 1280×950)가 됐고,
    포인터 매핑이 `*DPR`을 한 번 더 곱해 **잉크가 포인터의 정확히 2배 위치**에 찍혔다.
    마스크는 그 캔버스를 히어로 박스에 100%로 늘여 쓰므로 잉크와 어긋난 자리에 에코가 떴다.
    실측: dsf=1 잉크중심 (634,300)=정상 / dsf=2 (1269,600)=2배. **dsf=1에서만 우연히 맞아** 1~5회차 촬영을 통과했다.
  - **시공(`site/index.html`, CSS 3줄 + 직전 회차 에코 본체)**:
    ①`.hero-fx`에 `width:100%;height:100%` — 잉크·마스크 정합 복구(양 DPR에서 (634,300)).
    ②`.hero-echo .ic`를 자손까지(`,.hero-echo .ic *`) — `use`도 요소라 `.hero-echo *`에 직접 걸리고
      심볼의 `currentColor`는 **use에서 풀리므로** svg만 칠하면 아이콘 색이 안 바뀐다(규칙이 통째로 무효였다).
    ③`.hero-echo .pill-badge .ic`도 자손까지 `color:inherit` — 밝은 배지 위에 밝은 화살표가 얹혀
      **"성과 보기" 배지의 화살표가 아예 안 보이던** 문제 교정.
  - **기법 선택(게이트 5 구현노트)**: 잉크 캔버스를 80×60으로 축소해 알파를 뽑아 PNG 데이터URL 마스크로 씀.
    SVG stroke 마스크는 **버렸다** — 궤적을 따로 모델링하면 실제 잉크 농도(decay)와 어긋나 옅어진 자리에
    밝은 글자가 남는다. 캔버스에서 직접 뽑으면 농도가 항상 일치한다. `mix-blend-mode`는 배지·카드처럼
    자체 배경을 가진 요소에서 색이 뒤집혀 탈락.
  - **판정 = 합격.** 근접샷(`loop/shot-echo.mjs`) 4종 전부 잉크 위 밝은 글자로 또렷하게 읽힘
    (partners·sub·h1·cta). 규정 4장(wide/mobile × top/full) 통과 — 가로 넘침·겹침·폰트 이상 없음, 콘솔 오류 0.
    `reveal-clear`에서 잉크 소멸과 함께 에코도 사라지고 원본 어두운 글자 복귀 확인.
  - **크로스페이드 muddy band 없음(실측)**: 잉크 평균알파 0.929 → 0.206이 **250ms 안에** 끝나 무릎
    구간(.48~.72)은 순식간에 통과한다. t=+250ms 샷에서 원본 어두운 글자가 정상 가독.
  - **접근성 회귀 없음(실측)**: 에코 클론엔 진짜 `a`/`button`이 들어 있어 `aria-hidden`만으론 포커스가 안 막히는데,
    `inert`가 실제로 동작해 **에코 안 탭 스톱 0개**, 전체 17스톱으로 4회차 기록과 동일.
  - 용량 57,360→62,398B(+5,038B = +4.92KB, 상한 5KB 이내). 새 외부 의존 0.
- **2026-08-13 루프 5회차 — 정지(작업 없음)**
  - INBOX 지시 없음, 워킹트리 클린(중단된 직전 회차 산출물 없음), 큐 최상단이 "(비어 있음)".
  - 칸 2 규칙대로 `loop/STOP` 생성 후 종료. `site/index.html` 변경 0건, 촬영 없음.
  - **재가동 방법**: 큐에 작업을 채우거나 INBOX에 지시를 남기고 `loop/STOP`을 삭제.
    4회차까지 임무(히어로 리퀴드 리빌 부활 + 전반 폴리시)의 명시 후보는 모두 소진됐다 —
    다음 방향(예: 성능 예산·모바일 모션·카피 다듬기 등)은 본부장 결정 사항이라 루프가 스스로 고르지 않았다.
- **2026-08-13 루프 4회차 — 포커스 링·키보드 어포던스 (큐 1번: 이메일 CTA·푸터 마이크로 디테일)**
  - 실측으로 결함 2건 확인(탭 순회하며 `:focus-visible` 상태의 링 호스트·모서리·변형을 계측):
    ①**캡슐 버튼의 링이 직각 사각형** — 탭 대상인 `a.pill`/`button.pill`은 `border-radius:0`이고
      실제 둥근 박스는 자식 `.pill-in`(9999px)이라, 전역 `:focus-visible` 링이 캡슐 밖에 각지게 그려졌다.
      해당 3곳=히어로 "성과 보기"·"이메일 보내기", 푸터 "이메일 보내기".
    ②**키보드 포커스에 어포던스 0** — 모든 호버 반응이 `@media (hover:hover) and (pointer:fine)` 안의
      `:hover`에만 걸려 있어, 탭 사용자는 링만 받고 pill 스케일·화살표 넛지·`.alink` 이동을 전혀 못 받았다.
  - 시공(`site/index.html`, CSS만): 링을 `.pill`에서 **`.pill-in`으로 이설**(`.pill:focus-visible{outline:none}`
    + `.pill:focus-visible .pill-in{outline...;transform:scale(1.04)}`) — 링과 캡슐이 같은 박스에 걸려
    함께 scale되므로 어긋나지 않는다. 호버 블록 **밖에** `:focus-visible` 대응 규칙 추가
    (pill 스케일·배지 넛지 `--r`/`--ur`·outline 테두리·`.alink`·`.brand`·`.nav-b`·`.chip`).
    텍스트 링크는 2px 링이 글자에 붙어 답답해 `outline-offset` 확대(nav-b·brand 6px, alink 8px)+`border-radius:5px`.
  - 판정: 재실측 전 스톱에서 링 호스트=`span.pill-in`(9999px)·`hostTransform=scale(1.04)` 확인,
    배지/brand/nav-b/chip/alink 변형 전부 키보드에서 적용. 포커스 근접샷 눈판정(캡슐 3곳·chip·텍스트 링크) 통과.
    규정 4장(wide/mobile × top/full) 통과 — 레이아웃 변화 0(추가 CSS는 `:focus-visible` 상태 전용),
    가로 넘침·겹침 없음, 콘솔 오류 0. 용량 57,224→58,459B(+1,235B).
  - **부수 확인(결함 아님)**: 탭 순회 17스톱 전부 `visible=true`, 푸터 CTA로 포커스 스크롤도 정상(scrollY 4669).
    Lenis가 네이티브 포커스 스크롤을 가로채지 않는다.
- **2026-08-13 루프 3회차 — 스크롤 리빌 모션 타이밍 일관성 (큐 1번)**
  - 실측으로 결함 확인: 리빌은 요소별 IO인데 `--rd`는 "그룹이 한 화면에 같이 보인다"는 전제로 작성돼 있었다.
    아래 행일수록 홀로 화면에 들어오면서 작성된 딜레이만큼 빈칸으로 서 있게 됨.
    1440×900 기준 경력 10행이 그룹 첫 요소보다 102~973px 늦게 진입하면서 각각 80~760ms 대기,
    390×844에서는 성과 카드 4장(진입 간격 410~1261px)까지 같은 증상. 푸터 이메일 CTA도 200ms.
  - 시공(`site/index.html`, JS만 17줄): IO 콜백에서 **배치 상대 스태거로 정규화**.
    같은 부모 안에서 이번 배치에 함께 들어온 요소끼리만 상대 딜레이를 유지하고, 홀로 들어온 요소는 0ms.
    **마크업의 `--rd`는 손대지 않았다**(런타임에서만 덮어씀) — 스펙 확정 수치 보존.
  - 판정: 계단 스크롤(240px/200px 스텝) 실주행 검증 — 적용 딜레이 최대 360ms(타일 4장, 실제 동시 노출)로 수렴,
    경력·역량 행은 0~160ms. 리빌 누락 0건(전 요소 `is-in`+opacity 1), 콘솔 오류 0(양 뷰포트).
    눈판정 4장(wide/mobile × top/full) 통과 — 가로 넘침·겹침·폰트 이상 없음. 용량 56,313→57,224B(+911B).
- **2026-08-13 루프 2회차 — 모바일(390px) 히어로·성과 카드 여백/타이포 재점검 (큐 1번)**
  - 판정 = **합격, `site/index.html` 변경 0건.** 취향성 손질 없음(MASTER.md §0 "브리프가 이긴다").
  - 근거: ①가로 넘침 0(뷰포트 밖 요소는 skip 링크·게이트된 hero-fx 캔버스뿐 — 의도됨)
    ②카드 4장 실측 일관 — 패딩 26px·카드 간격 26px·워터마크 78px·h3 26px(스케일 rem 그리드 정상)
    ③눈판정: viewport 4장(wide/mobile × top/full) + 390px 클립 6장(#home·#works·카드 4장 개별) 전부 통과, 콘솔 오류 0.
  - `scripts/shot-clip.mjs` 신설: 셀렉터별 요소 클립 촬영(dpr2). 풀샷 7500px은 축소되어 디테일 판정 불가 —
    남은 폴리시 큐(모션·마이크로 디테일)도 이걸로 근접 판정할 것.
    카드는 `.cards li:nth-child(n)`으로 집을 것(`.card`는 li 안이라 nth-child가 안 먹힘).
- **2026-08-13 루프 1회차 — 히어로 리퀴드 리빌 부활 (한 묶음=1건)**
  - `site/index.html`: LUMORA_SPEC 2-A 메커니즘 그대로(brushRadius 143 · decay 0.016 · dpr≤2 ·
    idle 120프레임 하드클리어 · 브러시 스톱 1/.82/0 · step max(r*.3,1) · 보간 상한 60).
    리빌 레이어=오프스크린 캔버스 프로시저럴 잉크(#111→#0a0a0a 그라데이션 + #97501f/#b15f2c/#cf8047 결 110가닥,
    시드 고정 LCG라 매번 동일). 외부 에셋 0. 캔버스는 JS 생성이라 noscript 자동 제외,
    `!RM && (hover:hover and pointer:fine)` 게이트로 터치·reduced-motion 제외. idle 시 rAF 정지(배터리).
  - `scripts/shot-reveal.mjs` 신설: 로더 제거 대기 → S자 포인터 궤적 시뮬 → trail 캡처 → 2.6s 후 clear 캡처.
  - `scripts/shot-file.mjs` 보강: 고정 1200ms 대기가 로더 퇴장 구간에 걸려 **변경 전 HEAD도 빈 샷**이 찍히던
    함정 수정(로더 제거 대기+1700ms, 풀샷 전 리빌 종단상태 강제 — 풀페이지 캡처는 화면 밖 IO가 안 울림).
  - 판정: wide/mobile × top/full 4장 + reveal trail/clear 2장 전부 통과, 콘솔 오류 0. 용량 52,185→56,313B(+4.1KB, 상한 6KB 이내).

## 함정 목록
- main push = 39초 뒤 실사이트 발행. 루프는 loop-v23 브랜치 전용, push 금지.
- 🔴 **회당 제한은 40분**(08-13 20→40 상향). 큰 작업 1건이 20분에 잘려 **2회 연속 시간초과로 루프가 죽었다**.
  한 회차가 40분에도 안 끝날 크기면 작업을 쪼개서 큐에 다시 넣을 것 — 억지로 밀어붙이면 통째로 잘린다.
- 🔴 **시간초과로 죽으면 그 회차 출력 로그가 빈 채로 남는다**(강제 종료로 버퍼 소실).
  무슨 일이 있었는지는 `git log`·`git status`·STATUS 갱신분으로 재구성할 것. 로그가 비었다고 "아무것도 안 했다"가 아니다.
- 줄바꿈은 `.gitattributes`로 LF 고정(08-13). CRLF로 되쓰면 파일 전체가 변경으로 잡혀
  **더티 체크가 중단된 작업으로 오인**하고 diff가 수천 줄로 부풀려진다. 이 설정을 지우지 말 것.
- 스크린샷은 네트워크 필요(CDN 폰트·Lenis). 오프라인이면 스타일 없는 화면이 찍힘.
- shot-file.mjs 출력 파일명 고정 → 반복마다 새 폴더 필수.
- 640/1024/1440px 글자 스냅 = 수용된 이탈, 고치지 말 것.
- README.md는 v21 시절 내용으로 낡음 — 믿지 말 것(정본은 docs/DESIGN.md의 포인터).
- **정적 스크린샷에는 리퀴드 리빌이 안 보인다**(포인터 이벤트 필요) — shot-reveal.mjs로만 판정.
- 리빌 레이어에 외부 이미지 URL 금지(저작권·사진 0장 방침). 전부 코드 생성.
- **정적 스크린샷으로는 리빌 타이밍을 못 본다** — 촬영 스크립트가 `is-in`을 강제해 종단상태만 찍는다.
  타이밍 검증은 계단 스크롤 실주행(요소별 적용 `--rd`·`is-in` 회수)으로만 가능. 3회차 방식 참고.
- 리빌 `--rd`는 이제 **작성값 그대로 쓰이지 않는다**(런타임 배치 정규화, 08-13 3회차).
  마크업에서 큰 --rd를 새로 쓸 때 "같이 보이는 그룹인가"를 먼저 볼 것 — 아니면 어차피 0으로 깎인다.
- **Puppeteer `page.screenshot({clip})`은 문서 좌표계**다. `getBoundingClientRect()`(뷰포트 기준)를 그대로 넣으면
  scrollY=0인 헤더·히어로만 우연히 맞고 푸터는 엉뚱한 빈 영역이 찍힌다 — `+scrollX/+scrollY` 필수(08-13 4회차에 삭제).
  `scripts/shot-clip.mjs`는 `element.screenshot()`이라 이 함정이 없다(요소 기준). 좌표를 직접 넣을 때만 주의.
- 포커스 상태를 촬영할 땐 탭 직후 **1100ms 이상** 대기 — Lenis 스무스 스크롤이 안착하기 전 좌표를 읽으면 빗나간다.
- 호버 반응을 새로 추가할 땐 `@media (hover:hover) and (pointer:fine)` 안에만 쓰지 말 것 —
  키보드는 포인터와 무관해 그 블록에 갇히면 탭 사용자에게 아무 반응이 없다. `:focus-visible` 대응을 **미디어 밖에** 함께 쓸 것(08-13 4회차).
- 링을 그리는 요소와 둥근 박스가 다르면 링이 각지게 나온다(래퍼/자식 분리 패턴). 새 컴포넌트도 `.pill` 방식대로
  **둥근 자식에 outline을 얹을 것**.
- 🔴 **`canvas`는 대체요소다 — `inset:0`으로는 안 늘어난다.** `width/height:100%`를 반드시 함께 줄 것.
  없으면 CSS박스가 backing store 크기(=DPR배)가 돼 포인터 매핑이 어긋난다(08-13 6회차, 1회차부터 잠복).
  히어로에 캔버스를 새로 얹을 때 같은 실수를 반복하지 말 것.
- 🔴 **DPR 관련 판정은 반드시 `deviceScaleFactor: 2`로 촬영할 것.** `scripts/shot-file.mjs`·`shot-reveal.mjs`는
  dsf=1(`--force-device-scale-factor=1`)이라 위 2배 버그를 **5회차까지 못 잡았다**. 본부장 실화면(Windows 배율 125~150%)은
  dsf>1이다. 캔버스·마스크·좌표 계산을 건드렸으면 `loop/probe-dpr.mjs`로 dsf 1·2를 함께 볼 것.
- **SVG 아이콘 색은 `use`에서 풀린다.** `<svg class="ic"><use href="#i-…">` 구조라 `.ic`(svg)에만 color를 주면
  `use`가 상위의 다른 규칙을 받아 색이 안 바뀐다. `.sel .ic,.sel .ic *` 처럼 **자손까지** 지정할 것(08-13 6회차).
- **히어로 클론(`.hero-echo`)에는 진짜 `a`/`button`이 들어 있다.** `aria-hidden`은 포커스를 막지 못하므로
  `inert`가 필수다 — 지우면 탭 스톱이 즉시 2배가 된다. 클론을 다시 만들 때도 `inert`를 유지할 것.
- **`.row-note`와 `.row-sub`는 <1024px에서 크기·색이 완전히 같다**(.875rem/알파 .62~.65). 경력 섹션은 note를
  `row-note--mobile-hide`로 감춰서 안 겹치지만, **둘 다 보이게 쓰면 두 문단이 한 덩어리로 읽힌다**.
  새 행에 note+sub를 함께 쓸 땐 섹션 스코프로 sub를 낮출 것(`#skills .row-sub` 방식, 08-14 7회차).
- 🔴 **스크립트 `el.focus()`로는 `:focus-visible`이 안 뜬다.** 마지막 입력 모달리티가 마우스면(촬영 스크립트가
  앞서 `page.click()`을 했으면) 링이 안 그려져 **"링이 없다"는 오판**을 하게 된다. 링 촬영·판정은 반드시
  **실제 `Tab` 키를 눌러** 도달할 것(08-14 8회차에 한 번 헛다리 짚음). `matches(':focus-visible')`로 재확인.
- 🔴 **배경 스크롤 잠금에 `stopScroll()`을 재사용하지 말 것.** html에 `height:100%`를 걸어서 페이지 중간에서
  쓰면 문서 높이가 무너지고 스크롤이 0으로 튄다. 로더 전용(스크롤 0에서만 안전)이다.
  모달은 `html.is-modal{overflow:hidden;padding-right:var(--sbw)}` 방식을 쓸 것(08-14 8회차).
- 성과 카드 제목은 이제 `button.card-open`이다. `::after{inset:0}`가 카드 전체를 덮으므로 **카드 안에 새 링크·
  버튼을 넣으면 클릭이 안 먹는다**(겹침). 넣어야 하면 그 요소에 `position:relative`를 줘서 위로 올릴 것.
- 🔴 **"콘솔 오류 0"은 더 이상 기본 기준이 아니다(08-14 9회차부터).** `site/works/`가 비어 있으면 이미지 슬롯
  프로브가 뷰포트당 **12건**(카드 4 × 확장자 3)의 `ERR_FILE_NOT_FOUND`를 남긴다 — shot-file.mjs 2뷰포트 기준 **24건이 정상**.
  이미지가 채워질수록 줄고 4장 전부 jpg면 0이 된다. 판정 기준: **404가 아닌 오류가 1건이라도 있으면 불합격**,
  404는 개수와 경로(`works/*.jpg|png|webp`)만 확인할 것. 이 소음이 싫다면 확장자 후보를 줄이는 수밖에 없다.
- 카드에 새 레이어를 얹을 땐 `.card-meta`/`.card-wm`/`.card-bot`의 `z-index:1`을 확인할 것 —
  배경 슬롯(`.card-shot`, z-index 0)보다 위에 있어야 한다. `.card-open::after`(카드 전체 클릭 영역)는
  card-bot 안이라 슬롯보다 위에 있고, 슬롯은 `pointer-events` 기본값이라도 클릭을 가로채지 않는다(실측 확인).
- **빈 슬롯 카드의 404 개수는 "카드 수 × 3"이 아니다.** 이미 있는 파일도 확장자 순서(jpg→png→webp)에서
  앞의 것이 먼저 404를 낸다 — `nanakage.webp`는 jpg·png 2건을 남긴다. 10회차 실측 = 뷰포트당 5건
  (dalcom 3 + nanakage 2). 개수를 셀 때 이 규칙으로 예상치를 세울 것(08-14 10회차).
- **성과 카드 순서·개수는 `<ul class="cards">`의 `<li>` 순서가 전부다.** `--rd` 값은 3회차 런타임 정규화가
  덮어쓰므로 카드를 옮기거나 추가할 때 --rd를 맞추려 애쓸 필요가 없다(0/90/180…으로 이어 쓰면 충분).
- 카드를 추가·삭제하면 `loop/probe-modal.mjs`의 `KEYS` 배열도 같이 고쳐야 한다(커밋 제외 파일이라
  git diff에 안 잡힌다 — 안 고치면 없는 카드를 클릭하려다 프로브가 죽는다, 08-14 10회차).
- 🔴 **`hidden` 속성은 클래스의 `display`에 진다.** UA 스타일시트의 `[hidden]{display:none}`은 특이도가 0이라
  `.wdlg-fig{display:flex}` 같은 작성자 규칙이 이긴다 — `hidden`인데도 빈 칸이 남는다(08-14 12회차, 회사 카드
  6장 전부에 28px). `display`를 주는 클래스에는 `.그클래스[hidden]{display:none}`을 **반드시 같이 쓸 것**.
- **`loop/probe-modal.mjs`의 `open` 값을 믿지 말 것.** 클릭 후 700ms 계측에서 **전 카드(7장) 모두 `false`**로
  읽히지만 같은 순간의 스크린샷에는 다이얼로그가 멀쩡히 떠 있다(12회차 실측, 8회차부터 잠복한 계측 아티팩트).
  `probe-lab.mjs`처럼 1400ms를 기다리면 `true`가 나온다. 개폐 판정은 **스크린샷 + `closed`/`refocus`/`active`**로 할 것.
- 밝은 UI 스크린샷을 카드 배경으로 쓸 땐 9회차 스크림만으로는 부족하다 — 원본의 글자·숫자가 카드 카피와
  부딪힌다. `.lab-list .card-shot`처럼 **블러 + 진한 스크림**으로 분위기만 남기고 선명한 화면은 다이얼로그로
  보낼 것(08-14 12회차). 어두운 게임 아트(성과 카드 6장)는 기존 스크림 그대로 두면 된다.
- shot-file.mjs는 로더 제거를 기다린 뒤 찍는다(08-13 수정). **고정 sleep으로 되돌리지 말 것** — 로더 퇴장
  구간에 걸려 빈 샷이 나온다. 풀페이지 샷은 리빌 종단상태를 강제한 화면(레이아웃 판정용)임을 감안할 것.
