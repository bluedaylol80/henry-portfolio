# STATUS — 지금 위치·다음 할 일·완료 기록 (매 이터레이션 갱신, 인수인계서)

## 지금 위치
- v23 "Quiet Paper" 라이브(`main`=`05a238a`, 2026-08-12 배포). 루프 작업은 `loop-v23` 브랜치.
- **사전 질의 4건 답변 완료(2026-08-13 본부장 결재) — 큐 시드됨, 가동 준비 완료.**

## 본부장 사전 질의 — 답변 완료 (2026-08-13, HQ 세션 인터뷰)
1. 임무 = **히어로 리퀴드 리빌 부활(자체 비주얼) + 전반 폴리시**
2. 기준 = 이터레이션 내 눈판정 통과 → 최종 합격은 본부장 실화면(PC+폰) 판정. 배포는 본부장 결재 전용.
3. 상한 = 기본 10회.
4. v23 사후 퀴즈 = **불필요** — 퀴즈는 배포 전에 실시됨(08-12 HQ 세션: 1차 0/3 → 해설 → 재퀴즈 3/3 만점 → 머지·배포). 기록=해당 세션 대화, 위반 아님.

## 다음 할 일 큐 (최상단 1건만 집는다)
1. **히어로 리퀴드 리빌 부활 (검증 하니스 포함, 한 묶음=1건)** —
   `design/reference/LUMORA_SPEC.md` "2) Hero — A) LiquidReveal" 메커니즘·수치 그대로
   (brushRadius 143 · decay 0.016 · dpr≤2 · idle 120프레임 하드클리어 · 브러시 그라데이션 스톱 1/.82/0).
   단 **사진 2장 대신 코드로 그린 자체 레이어**: 베이스=현 히어로(유지),
   리빌 레이어=오프스크린 캔버스에 프로시저럴 생성(잉크 `#0a0a0a` 계열+버트오렌지 결 — DESIGN 팔레트 내 색만).
   외부 이미지 URL·새 CDN·새 라이브러리 금지. 터치·reduced-motion·noscript 제외(기존 fail-safe 패턴 유지).
   용량 증가 상한 +6KB. **검증**: 정적 스샷엔 포인터 효과가 안 찍히므로 `scripts/shot-reveal.mjs` 신설
   (포인터 궤적 시뮬 후 히어로 캡처) — 이 스샷 눈판정까지 통과해야 완료.
2. 폴리시: 모바일(390px) 히어로·성과 카드 여백/타이포 디테일 재점검 (스샷 눈판정 기준).
3. 폴리시: 스크롤 리빌 모션 타이밍 일관성 — 섹션 간 딜레이·스태거 점검.
4. 폴리시: 이메일 CTA·푸터 마이크로 디테일 (호버 상태·포커스 링·:focus-visible).

## 완료 기록
- (없음 — 루프 미가동)

## 함정 목록
- main push = 39초 뒤 실사이트 발행. 루프는 loop-v23 브랜치 전용, push 금지.
- 스크린샷은 네트워크 필요(CDN 폰트·Lenis). 오프라인이면 스타일 없는 화면이 찍힘.
- shot-file.mjs 출력 파일명 고정 → 반복마다 새 폴더 필수.
- 640/1024/1440px 글자 스냅 = 수용된 이탈, 고치지 말 것.
- README.md는 v21 시절 내용으로 낡음 — 믿지 말 것(정본은 docs/DESIGN.md의 포인터).
- **정적 스크린샷에는 리퀴드 리빌이 안 보인다**(포인터 이벤트 필요) — shot-reveal.mjs로만 판정.
- 리빌 레이어에 외부 이미지 URL 금지(저작권·사진 0장 방침). 전부 코드 생성.
